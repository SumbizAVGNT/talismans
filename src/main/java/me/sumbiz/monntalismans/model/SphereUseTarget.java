package me.sumbiz.monntalismans.model;

public enum SphereUseTarget {
    PLAYER, // точка = игрок
    BLOCK,  // точка = кликнутый блок (если есть)
    HIT     // точка = место попадания проектиля
}
