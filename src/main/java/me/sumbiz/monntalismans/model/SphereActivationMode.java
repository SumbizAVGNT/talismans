package me.sumbiz.monntalismans.model;

public enum SphereActivationMode {
    RIGHT_CLICK,  // ПКМ и в воздух и по блоку
    AIR_ONLY,     // только ПКМ в воздух
    BLOCK_ONLY,   // только ПКМ по блоку
    THROW         // бросок проектилем
}
